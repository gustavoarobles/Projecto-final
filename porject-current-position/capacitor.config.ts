import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'porject-current-position',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
