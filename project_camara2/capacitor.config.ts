import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miempresa.myApp',
  appName: 'project_camara2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
